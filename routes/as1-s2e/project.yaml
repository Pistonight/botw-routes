title: All Shrines (S2E)
version: v1.0
config:
- plugins:
  - use: split-format
    with:
      Shrines: "[.var(pad03:counter-shrine)] .prop(text)"
      Warps: "Warp .prop(text)"
- use: Pistonight/celer-presets/botw/most.yaml
- use: ../../common/config.yaml
route:
  use: ./main.yaml